export const PrimaryID = ({ children }) => (
  <div style={primaryIdStyle}>{children}</div>
);
export const primaryIdStyle: React.CSSProperties = {
  fontWeight: 500,
  fontSize: '16px',
  lineHeight: '20px',
  textAlign: 'left',
};
